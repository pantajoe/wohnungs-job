removeLoadingSpinner();

$('.reports-list').html("<%=j render(partial: 'report', collection: @reports) %>");

// Highlight the created report
$($('.reports-list').children()[0]).effect('highlight', { color: '#FCD54A' }, 3000);

// Initialize Event Listeners
initializeShowReport();
initializeSendMail();
